@model IEnumerable<WebsiteBanHangQuanAoNam.Models.Danhmuc>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Ten)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Ten)
            </td>
            <td>
                    <a Class="btn btn-outline-warning" asp-action="Edit" asp-route-id="@item.MaDm">Sửa</a> |
                    <a Class="btn btn-outline-primary " asp-action="Details" asp-route-id="@item.MaDm">Thông Tin</a> |
                    <a Class="btn btn-outline-danger" asp-action="Delete" asp-route-id="@item.MaDm">xóa</a>
            </td>
        </tr>
}
    </tbody>
</table>
